# Rust Sum

A simple Rust program that reads two whole numbers and prints their sum.

## Run

Install Rust, then compile and run:

```bash
rustc sum.rs -o sum
./sum
```

Enter each number when prompted.
